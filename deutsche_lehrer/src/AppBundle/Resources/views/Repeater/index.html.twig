{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Repeater list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Repeatdate</th>
                <th>Repeatcode</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('repeater_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.repeatDate %}{{ entity.repeatDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.repeatCode }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('repeater_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('repeater_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('repeater_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
