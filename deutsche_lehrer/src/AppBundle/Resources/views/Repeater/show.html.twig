{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Repeater</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Repeatdate</th>
                <td>{{ entity.repeatDate|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Repeatcode</th>
                <td>{{ entity.repeatCode }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('repeater') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('repeater_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
